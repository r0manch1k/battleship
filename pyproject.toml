[project]
name = "battleship"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.6",
    "pillow>=12.3.0",
    "ruff>=0.15.20",
]
